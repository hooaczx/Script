local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ScaleModeGui"
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0.4, 0, 0.35, 0)
mainFrame.Position = UDim2.new(0.3, 0, 0.35, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
mainFrame.BackgroundTransparency = 0.5
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local titleBox = Instance.new("Frame")
titleBox.Size = UDim2.new(1, 0, 0.2, 0)
titleBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
titleBox.BorderSizePixel = 0
titleBox.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 1, 0)
titleLabel.Text = "Notifications"
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextScaled = true
titleLabel.BackgroundTransparency = 1
titleLabel.Parent = titleBox

local instructionsLabel = Instance.new("TextLabel")
instructionsLabel.Size = UDim2.new(1, -20, 0.4, 0)
instructionsLabel.Position = UDim2.new(0, 10, 0.25, 0)
instructionsLabel.Text = "you want to Execute lnfinitive Ui Script"
instructionsLabel.Font = Enum.Font.Gotham
instructionsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
instructionsLabel.TextScaled = true
instructionsLabel.BackgroundTransparency = 1
instructionsLabel.TextWrapped = true
instructionsLabel.Parent = mainFrame

local loadingButton = Instance.new("TextButton")
loadingButton.Size = UDim2.new(1, 0, 0.2, 0)
loadingButton.Position = UDim2.new(0, 0, 0.65, 0)
loadingButton.Text = "loading Gui"
loadingButton.Font = Enum.Font.GothamBold
loadingButton.TextColor3 = Color3.fromRGB(255, 255, 255)
loadingButton.TextScaled = true
loadingButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
loadingButton.Parent = mainFrame

local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(1, 0, 0.2, 0)
closeButton.Position = UDim2.new(0, 0, 0.85, 0)
closeButton.Text = "Close"
closeButton.Font = Enum.Font.GothamBold
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextScaled = true
closeButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
closeButton.Parent = mainFrame

loadingButton.MouseButton1Click:Connect(function()
loadingButton.Text = "loading..."
screenGui:Destroy()
loadstring(game:HttpGet("https://raw.githubusercontent.com/hooaczx/Script/main/Lihh"))()
end) 


closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)
